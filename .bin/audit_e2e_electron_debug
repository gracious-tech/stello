#!/bin/bash
# Run tests for electron in dev mode (unpackaged electron + dev app server)
set -e
cd "$(dirname "$(dirname "$0")")"


# Build electron source before running tests
.bin/build_electron

cd electron/tests
DEV=1 ../../node_modules/.bin/playwright "${@:-test}"
