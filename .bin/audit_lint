#!/bin/bash

# Lint everything
cd `dirname "$0"`
cd ..

# NOTE ironically the TS plugin has trouble parsing .eslintrc.js!
node_modules/.bin/eslint --ext .js,.ts,.vue --ignore-path=.gitignore --ignore-pattern=".eslintrc.js" "$@" .
