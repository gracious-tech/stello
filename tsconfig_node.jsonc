// NOTE vscode will warn about trailing commas in this file, but can ignore the warnings
{
    "$schema": "https://json.schemastore.org/tsconfig",
    "extends": "./tsconfig_base.jsonc",
    "compilerOptions": {
        // Based on Node 14 (review when upgraded)
        "lib": ["ES2020"],
        "module": "CommonJS",
        "target": "ES2020",
        "types": ["node"],  // Node is system-wide, where as all others must be per component
    },
}
